function booWho(bool) {
	return typeof bool === 'boolean';

	//   if (bool == true || bool == false) { 
	//     return true; 
	//    } else if (bool == 1) { //could not get this to work
	//      return false;
	//   } else {
	//     return false;
	//   }
	//   return bool;
}

booWho(null);
